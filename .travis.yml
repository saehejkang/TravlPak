
language: node_js
node_js: "12.16.1"
cache: npm
before_install:
install: install.sh
script: build.sh

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
